INPUT ORDER MODELS:
base_v1: x,y s.b. constraint + two_stack
base_v2: x,y s.b. constraint + two_stack + three_stack
base_v3: largest circuit on 0,0 + two_stack
base_v4: largest circuit on 0,0 + two_stack + three_stack

DOM_W_DEG MODELS:
base_v5: x,y s.b. constraint + two_stack
base_v6: x,y s.b. constraint + two_stack + three_stack
base_v7: largest circuit on 0,0 + two_stack
base_v8: largest circuit on 0,0 + two_stack + three_stack

In the models with "largest circuit on 0,0",
I added that in the stacking constraints we will not consider the block on 0,0
=> This avoids deleting some valid solutions